# The assessment of MODEL-ROUTE-001 against this build. The requirement identifier is
# this file's stem and appears nowhere inside it, because a second place to spell the
# identity is a second place for it to be wrong -- the rule OD-SPEC-007 chose for a
# record registration, and CHK-003.assessment states for this registry.
#
# MODEL-ROUTE-001: "Gates, phases, workflows, rules/checks, and agent task classes shall
# be able to reference a ModelExecutionProfile for every agent-assisted operation. The
# smallest explicit assignment shall override inherited defaults subject to organization
# and repository policy constraints."
#
# One of five referencing surfaces is real: GateCommand::model is a genuine, tested field
# a gate can carry a profile through. The other four are not -- rules/checks
# (nomos_contracts::RuleId), phases, workflows and agent task classes have no structured
# type anywhere in this workspace to hold a reference at all, named in
# ModelExecutionProfile's own doc comment. The second sentence (override precedence) has
# no resolver anywhere to carry it out.
verdict: Partial
site: crates/orchestration/nomos-gate-orchestration/src/command.rs#GateCommand
gap: crates/packages/nomos-model-package/src/model_execution_profile.rs#ModelExecutionProfile

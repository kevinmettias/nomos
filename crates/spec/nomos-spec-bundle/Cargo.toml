[package]
name = "nomos-spec-bundle"
version = "0.1.0"
description = "Band 1. The portable specification bundle: deterministic JSONL export and import."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
# The determinism vocabulary, for the one declaration in `src/determinism.rs`. Band 0,
# and the crate every non-Rust peer reimplements — which is why the declaration lives
# here and the machinery that checks it lives in `tests/integration`.
nomos-contracts = { workspace = true }
nomos-spec-model = { workspace = true }
nomos-spec-store = { workspace = true }
base64 = { workspace = true }
rusqlite = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }

[lints]
workspace = true

//! What this module promises, exercised.
//!
//! Split by the subject each test is about -- the codes this group exits with, the command
//! line it accepts, and the real runs it performs over this workspace's own tree -- because
//! what a reader comes here to find is the subject rather than the assertion.

mod exit_codes;
mod invocation;
mod real_runs;

[package]
name = "nomos-workspace"
version = "0.1.0"
description = "What the workspace currently is, and the one door every change comes through."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
nomos-contracts = { workspace = true }
nomos-model = { workspace = true }
nomos-store = { workspace = true }

[lints]
workspace = true

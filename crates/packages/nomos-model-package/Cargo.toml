[package]
name = "nomos-model-package"
version = "0.1.0"
description = "The first ModelBackendPackage/AgentExecutorPackage manifest maturity: identity, PackageKind, PKG-007's first two version domains reused from nomos-package unchanged, and ModelSelection replacing the fourth."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
nomos-contracts = { workspace = true }
nomos-package = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }

[lints]
workspace = true

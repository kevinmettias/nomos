[package]
name = "nomos-package"
version = "0.1.0"
description = "Band 24. The language-agnostic core of an installable-unit manifest -- PKG-007's four version domains, minus any typed version-domain abstraction or provider allowlist a specific language would supply."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
nomos-contracts = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }

[lints]
workspace = true

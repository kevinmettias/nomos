[package]
name = "nomos-lang-go-package"
version = "0.1.0"
description = "Band 26. The Go LanguagePackage manifest format, wrapping nomos-package's generic core with GoVersion resolution and this workspace's one Go syntax provider -- PKG-007's four version domains, kept distinct."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
nomos-contracts = { workspace = true }
nomos-package = { workspace = true }
nomos-lang-go = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }

[lints]
workspace = true

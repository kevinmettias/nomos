[package]
name = "nomos-repo-standards-document"
version = "0.1.0"
description = "Band 24. The shared standards.json acquisition step OD-RULES-019 decided the five nomos.cap.*.policy providers owe one read/parse boundary: STANDARDS_JSON, Read_To_String plus serde_json::from_str, and one error type. No semantic extraction -- that stays in each provider's own reading.rs."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
nomos-platform = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
nomos-platform-std = { workspace = true }

[lints]
workspace = true

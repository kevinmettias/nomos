[package]
name = "nomos-agent-executor"
version = "0.1.0"
description = "Band 37. The first real AgentExecutor: dispatches a TaskEnvelope's goal to Claude Code as a subprocess, bounded by OD-EXECUTOR-001's structural capability boundary, and reads back an AgentExecutionOutcome."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
nomos-agent-contracts = { workspace = true }
nomos-platform = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
nomos-contracts = { workspace = true }
nomos-ledger = { workspace = true }
nomos-platform-std = { workspace = true }

[lints]
workspace = true

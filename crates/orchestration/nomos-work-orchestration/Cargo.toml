[package]
name = "nomos-work-orchestration"
version = "0.1.0"
description = "Band 40. Runs a `nomos work` verb against a caller-supplied ledger and platform, and hands back what happened -- the seam a second adapter calls without choosing a platform or rendering the answer."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
nomos-ledger = { workspace = true }
nomos-platform = { workspace = true }

[dev-dependencies]
nomos-platform-std = { workspace = true }

[lints]
workspace = true

[package]
name = "nomos-gate-orchestration"
version = "0.1.0"
description = "Band 40. The seam for the first-class Gate object ARC-ROADMAP-001 names, starting with Plan: wiring a real rule registry and reporting what it holds, apart from choosing a platform, selecting scope or rendering the answer."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
nomos-contracts = { workspace = true }
nomos-rules = { workspace = true }

[lints]
workspace = true

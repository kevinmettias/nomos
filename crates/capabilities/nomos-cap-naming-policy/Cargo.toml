[package]
name = "nomos-cap-naming-policy"
version = "0.1.0"
description = "The nomos.cap.naming.policy contract: a repository's own declared naming convention, read as data rather than compiled as a constant. OD-RULES-011."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

# nomos-rules is this capability's second real party from day one, the same
# OD-CAPABILITY-002 contention criterion nomos-cap-lint and nomos-cap-dependency-policy
# were already extracted under.
[dependencies]
nomos-capability = { workspace = true }
nomos-contracts = { workspace = true }

[lints]
workspace = true

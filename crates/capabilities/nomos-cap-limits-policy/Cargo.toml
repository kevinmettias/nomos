[package]
name = "nomos-cap-limits-policy"
version = "0.1.0"
description = "The nomos.cap.limits.policy contract: a repository's own declared numeric thresholds (file-size triggers, parameter-count caps, and their like), read as data rather than compiled as a constant. OD-RULES-011."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

# nomos-rules is this capability's second real party from day one, the same
# OD-CAPABILITY-002 contention criterion nomos-cap-naming-policy was already extracted
# under for the sibling family OD-RULES-011 names.
[dependencies]
nomos-capability = { workspace = true }
nomos-contracts = { workspace = true }

[lints]
workspace = true

[package]
name = "nomos-cap-scripting-policy"
version = "0.1.0"
description = "The nomos.cap.scripting.policy contract: a repository's own declared tooling language and forbidden script extensions, read as data rather than compiled as a constant. OD-RULES-011."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
nomos-capability = { workspace = true }
nomos-contracts = { workspace = true }

[lints]
workspace = true

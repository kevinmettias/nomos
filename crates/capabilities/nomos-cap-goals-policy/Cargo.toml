[package]
name = "nomos-cap-goals-policy"
version = "0.1.0"
description = "The nomos.cap.goals.policy contract: the purposes a repository declares, the parts it declares to serve them, and how thinly one purpose may be spread, read as data rather than compiled as a constant. OD-RULES-011."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
nomos-capability = { workspace = true }
nomos-contracts = { workspace = true }

[lints]
workspace = true

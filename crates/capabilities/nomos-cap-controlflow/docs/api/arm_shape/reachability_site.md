# API — arm_shape/reachability_site

## Contract

Absence from a payload is not a claim of cleanliness: an arm whose body is anything else is
simply not recorded, the same "no finding is not confirmed clean" convention every rule in
this workspace already keeps.
